import './bootstrap';
import $ from "jquery";
$(document).ready(function () {
    // Echo.private(`approved-stories`).listen('StoryApproved', (data) => {
    //     console.log(data)
    //     const arr = data.story;
    //         let id = arr.id;
    //         let tagId = 'story_' + id;
    //         if (!$('#' + tagId).length) {
    //             $('.story-container').append(
    //                 '<div class="story" id="' + tagId + '">' +
    //                 '<h3>' + arr.title + '</h3>' +
    //                 arr.description +
    //                 '</div>'
    //             );
    //         }
    //
        // });
})
